# Copie para `.env.local` (ou `.env`) e ajuste. Nunca versione o arquivo preenchido.
# A configuração completa de ambiente é formalizada na issue #9.

# Banco primário local — SQLite (D24). Caminho relativo a `apps/web/prisma/`.
DATABASE_URL="file:../data/latexbookbank.db"
